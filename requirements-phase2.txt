# Phase 2 Optimization Dependencies
# Optional performance and security enhancements
# Install with: pip install -r requirements-phase2.txt

# Async HTTP for concurrent WMS fetching (50% faster startup)
aiohttp==3.10.11

# Rate limiting for API protection
Flask-Limiter==3.8.0

# Redis caching (optional - falls back to simple cache if not installed)
redis==5.2.1
Flask-Caching[redis]==2.3.0

# Security enhancements
Flask-Talisman==1.1.0  # HTTPS enforcement and security headers

# Performance monitoring
py-spy==0.3.14  # CPU profiling
memory-profiler==0.61.0  # Memory profiling

# Additional development tools
bandit==1.8.0  # Security linting
safety==3.2.11  # Dependency vulnerability scanning
