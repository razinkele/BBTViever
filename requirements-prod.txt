# Production-only dependencies
-r requirements.txt

# Production WSGI server
gunicorn==23.0.0

# Optional: Redis for distributed caching (if needed)
# Uncomment if using Redis backend for Flask-Caching
# redis==5.2.1

# Optional: Environment variable management
# Uncomment if using .env files in production
# python-dotenv==1.1.1
